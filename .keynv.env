# .keynv.env — alias references to vault secrets.
# Safe to commit: this file contains references, not values.
# Auto-loaded by `keynv exec`. See https://keynv.dev/docs/keynv-env

# Vault-resolved (real values live on the keynv server)
JWT_SECRET=@keynv-test.dev.jwt-secret
