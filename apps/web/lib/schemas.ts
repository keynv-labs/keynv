import { validation } from '@keynv/core';

export const {
  EnvSpec,
  approvalReason,
  cliTokenName,
  email,
  envName,
  envTier,
  expiresInSeconds,
  orgId,
  orgName,
  orgRole,
  password,
  passwordMin12,
  projectId,
  projectName,
  projectRole,
  requireApproval,
  secretKey,
  secretValue,
  testerName,
} = validation;
